name: Django CI

on:
  push:
    branches:
      - dev # Trigger CI on push to the dev branch
  # pull_request:
  #   branches:
  #     - main # Uncomment this to enable PR checks for main branch

jobs:
  install-dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate

      - name: Install dependencies
        run: |
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Cache virtual environment
        uses: actions/cache@v2
        with:
          path: venv
          key: venv-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            venv-
    
  lint:
    runs-on: ubuntu-latest
    needs: install-dependencies  # Ensures this job runs after install-dependencies
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Restore virtual environment cache
        uses: actions/cache@v2
        with:
          path: venv
          key: venv-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            venv-

      - name: Activate virtual environment and run linters
        run: |
          source venv/bin/activate  # Activate virtual environment
          pip freeze
          # Ensure the environment is activated and tools are available
          which flake8
          which isort
          which black

          # Run the linting tools
          flake8 .
          isort --check-only --diff
          black --check .
